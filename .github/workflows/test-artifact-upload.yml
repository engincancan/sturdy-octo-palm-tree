name: Test Artifact Upload

on:
  push:
    branches:
      - main
      - copilot/**
  pull_request:
  workflow_dispatch:

jobs:
  test-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - run: mkdir -p /Users/runner/work/homebase/homebase/applications/consumer-native-app/ios/build/output/Armut

      - run: echo hello > /Users/runner/work/homebase/homebase/applications/consumer-native-app/ios/build/output/Armut/Armut.txt

      - uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: /Users/runner/work/homebase/homebase/applications/consumer-native-app/ios/build/output/Armut/Armut.txt
